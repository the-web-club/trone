import type { Metadata } from "next";
import { ComingSoon } from "@/components/shell/coming-soon";

export const metadata: Metadata = { title: "Producten" };

export default function ProductenPage() {
  return <ComingSoon title="Producten" />;
}
