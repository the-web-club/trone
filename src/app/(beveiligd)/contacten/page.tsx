import type { Metadata } from "next";
import { ComingSoon } from "@/components/shell/coming-soon";

export const metadata: Metadata = { title: "Contacten" };

export default function ContactenPage() {
  return <ComingSoon title="Contacten" />;
}
